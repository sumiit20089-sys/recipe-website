# RecipeNest Images Directory

This directory is intended for local image assets. 
In the current implementation, RecipeNest utilizes high-quality Unsplash CDN URLs for all food photography to ensure fast page load times and keep the repository size lightweight.

If you wish to deploy local images, place them in this folder and update the `src` attributes in the HTML files accordingly.
